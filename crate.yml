# Crate Configuration

network.host: _local_,_site_

# Paths
path:
  logs: /data/log
  data: /data/data
blobs:
  path: /data/blobs

auth:
  host_based:
    enabled: true
    config:
      0:
        user: crate
        method: trust
      1:
        method: password